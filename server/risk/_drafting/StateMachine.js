
const StateTransition = ({ startingState, endState, condition}) => ({
    startingState,
    endState,
    condition
})

const RiskGameState = ({ name }) => {
    
    return {
        name
    }
}

const setupTo

// stupid simple
const StateMachine = (states) => {
    let currentState = null;
    const stateTransitions = []


    const getCurrentState = () => {


    } 

    const tick = () => {
        // is there a state transition whose condition passes?
    }




}